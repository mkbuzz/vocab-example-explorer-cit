name: Apply flashcard upgrade

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-flashcard-upgrade.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  upgrade:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply flashcard upgrade
        run: python .github/scripts/upgrade_flashcards.py
      - name: Commit updated Vocabulary Explorer
        run: |
          if git diff --quiet -- index.html; then
            echo 'No changes needed.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git commit -m 'Add flashcard-style vocabulary cards'
          git push
